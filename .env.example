# 로컬 개발 기본값 — 그대로 .env 로 복사해서 사용
DATABASE_URL=postgresql://socar:socar@localhost:5432/socar
JWT_SECRET=local-dev-secret-change-me
API_PORT=4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# 5432를 이미 다른 컨테이너가 쓰고 있으면 이 값만 바꾼다 (docker-compose가 읽는다).
# 바꿨다면 위 DATABASE_URL의 포트도 같이 맞춰야 한다.
# DB_PORT=5434
