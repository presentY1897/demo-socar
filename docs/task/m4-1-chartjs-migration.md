# M4-1 — Chart.js 전면 전환

- 마일스톤: M4 (지표·차트·Export) · 규모 M
- 상태: ☐ 대기
- 의존: M3 완료

## 목적

**Chart.js 전면 통일로 확정**(Q6, 2026-09-01) — 기존 Recharts 차트를 포함해 전 차트를 Chart.js로 교체하고 라이브러리를 하나로 정리한다.

## 작업 내용

- [ ] `chart.js` 의존성 추가 (react-chartjs-2 또는 직접 canvas 래핑 — 번들 크기 비교 후 선택)
- [ ] 공용 차트 래퍼 컴포넌트: 프로젝트 컬러 토큰 적용 · 반응형 리사이즈 · 로딩/빈 데이터 상태 · 한국어 축/툴팁 포맷(원화, 날짜)
- [ ] 기존 회계 탭의 일별 차트(Recharts)를 래퍼로 교체
- [ ] `recharts` 의존성 제거 (`package.json` + import 전수 확인)
- [ ] M3 화면 중 차트가 어울리는 자리에 적용 (예: 회계 손익 추이, 핸들러 처리량)

## 산출물

- `apps/web/src/components/charts/`(래퍼) · 회계 탭 차트 교체 · package.json 정리

## 완료 기준

- `grep -r recharts apps/web` 결과 0건, 빌드 통과
- 전 차트가 라이트/다크 어디서든 렌더 (기존과 동일 데이터로 육안 대조)

## 테스트

- **백엔드**: 해당 없음
- **프론트(MSW)**: 차트 래퍼 — 데이터 있음/빈 데이터/로딩 3상태 렌더 · 원화·날짜 포맷 스냅샷 · 교체된 회계 차트가 MSW 지표 데이터로 렌더

## 참고

- [project-review.md](../project-review.md) §5.5
