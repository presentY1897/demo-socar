# M2-6 — M2 검증·문서·배포

- 마일스톤: M2 (핸들러 시스템) · 규모 S
- 상태: ☐ 대기
- 의존: M2-2 ~ M2-5

## 목적

M2 마일스톤 마감 — 부름 전 과정이 사람(핸들러)의 작업으로 이어지는 흐름을 검증하고 배포한다.

## 작업 내용

- [ ] 통합 테스트: 부름 전 과정 시나리오 — 부름 예약 → DELIVERY 자동 생성 → 배정 → 완료 → 이용 → 반납 → RETRIEVE 생성 → 완료 → 차량 원래 존 복귀
- [ ] 백엔드 + 프론트(MSW) 테스트 전건 통과
- [ ] **ADR-008 작성** — "핸들러 작업 모델": 작업 자동 생성 시점을 예약 트랜잭션에 묶은 이유, 완료 시 물리 위치 갱신과 위치 체인(ADR-005)의 역할 분담
- [ ] README 갱신: 데모 계정 표 5종(핸들러 추가), 기능 섹션에 핸들러 항목
- [ ] `SEED_VERSION` 확인 → main 머지 → 푸시 → 배포 스모크 (핸들러 계정 동선 완주)
- [ ] work-plan 인덱스·작업 문서 상태 갱신

## 산출물

- `docs/adr/008-handler-task-model.md`(신규) · README 갱신 · 배포

## 완료 기준

- 배포 URL에서 개인 계정(부름 예약)과 핸들러 계정(배달 완료)을 오가며 전 과정 완주

## 참고

- [work-plan.md](../work-plan.md) 작업 단위 규칙
